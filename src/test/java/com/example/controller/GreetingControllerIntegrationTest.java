package com.example.controller;

import io.quarkus.test.junit.NativeImageTest;

//@NativeImageTest
public class GreetingControllerIntegrationTest //extends GreetingControllerTest
{
    // Run the test using a native image rather than in JVM
    // Path to native executable is set using "native.image.path"
}
